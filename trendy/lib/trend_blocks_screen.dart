import 'package:flutter/material.dart';

class TrendBlocksScreen extends StatelessWidget {
  const TrendBlocksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TrendBlocks'),
      ),
      body: const Center(
        child: Text('TrendBlocks Screen'),
      ),
    );
  }
}
